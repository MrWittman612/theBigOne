cont initailState = {
isAuthenticated: false,
user: {}
}

export default function(state = initailState, action){
  switch (action.type) {
    // case expression:
    //
    //   break;
    default:
      return state;
  }
}
